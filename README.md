# GOLDIP Tunnel Manager 🚀

مدیریت حرفه‌ای تانل‌های Backhaul با رابط کاربری زیبا

## نصب و اجرای فوری ⚡

### روش 1: نصب خودکار (توصیه می‌شود) ⭐

فقط یک دستور کافیه که همه چیز خودکار نصب و اجرا بشه:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/skyboy610/goldip/main/install.sh)
```

یا با wget:

```bash
bash <(wget -qO- https://raw.githubusercontent.com/skyboy610/goldip/main/install.sh)
```

### روش 2: نصب دستی

```bash
# دانلود
wget https://raw.githubusercontent.com/skyboy610/goldip/main/goldip.sh -O /usr/local/bin/goldip

# قابل اجرا کردن
chmod +x /usr/local/bin/goldip

# اجرا
goldip
```

### روش 3: دانلود و اجرای موقت (بدون نصب)

```bash
wget https://raw.githubusercontent.com/skyboy610/goldip/main/goldip.sh && chmod +x goldip.sh && ./goldip.sh
```

## دستورات اجرا 🎯

بعد از نصب، می‌توانید از این دستورات استفاده کنید:

```bash
goldip        # دستور اصلی
gip           # دستور کوتاه
```

## ویژگی‌های اصلی ✨

- ✅ **6 پروتکل پشتیبانی شده**: TCP, TCPMUX, WS, WSS, GRPC, UDP
- ✅ **نام‌گذاری تانل‌ها**: هر تانل یک نام اختصاصی دارد
- ✅ **بهینه‌سازی NAT**: مخصوص سرورهای ایران پشت NAT
- ✅ **سرعت بالا**: با تنظیمات پیشرفته TCP و BBR
- ✅ **پایداری عالی**: مقاومت در برابر ترافیک سنگین
- ✅ **رابط زیبا**: رنگی و کاربرپسند
- ✅ **مدیریت کامل**: Start, Stop, Restart, Edit, Delete

## پروتکل‌های پشتیبانی شده 🌐

| شماره | پروتکل | کاربرد |
|-------|--------|--------|
| **1** | TCP | قابل اعتماد و سنتی |
| **2** | TCPMUX | چندگانه‌سازی TCP |
| **3** | WS | WebSocket - عبور از فایروال |
| **4** | WSS | WebSocket Secure - امن |
| **5** | GRPC | کارایی بسیار بالا |
| **6** | UDP | سرعت بالا برای گیمینگ |

## تنظیمات بهینه‌سازی ⚙️

### سرور ایران (پشت NAT)
```
Channel Size: 8192
Heartbeat: 20 ثانیه
MUX Sessions: 16
BBR Congestion Control: فعال
TCP Fast Open: فعال
```

### سرور خارج (Client)
```
Channel Size: 8192
Connection Pool: 32
Aggressive Pool: فعال
Keepalive: 15 ثانیه
TCP Fast Open: فعال
```

## آموزش استفاده 📖

### ۱. نصب Backhaul
در منوی اصلی گزینه `1` را انتخاب کنید

### ۲. ساخت تانل Iran (Server)

```
1. منوی اصلی → گزینه 2 (Add Tunnel)
2. انتخاب 1 (Iran Server)
3. نام تانل: مثلاً MyTunnel
4. انتخاب پروتکل: مثلاً 1 (TCP)
5. پورت تانل: مثلاً 443
6. Token: خودکار تولید می‌شود (کپی کنید!)
7. پورت‌های VPN: مثلاً 8080,8443
```

### ۳. ساخت تانل Kharej (Client)

```
1. منوی اصلی → گزینه 2 (Add Tunnel)
2. انتخاب 2 (Kharej Client)
3. نام تانل: مثلاً ClientTunnel
4. انتخاب پروتکل: مثلاً 1 (TCP)
5. IP ایران: مثلاً 1.2.3.4
6. پورت تانل: 443
7. Token: کپی شده از سرور ایران
```

## منوی اصلی 📋

```
[1] Install Backhaul    - نصب Backhaul
[2] Add Tunnel          - ساخت تانل جدید
[3] Manage Tunnel       - مدیریت تانل‌ها
[4] Logs                - مشاهده لاگ‌ها
[5] Tunnel Status       - وضعیت تانل‌ها
[6] Uninstall           - حذف کامل
[7] Exit                - خروج
```

## مدیریت تانل‌ها 🔧

در منوی Manage Tunnel:

- [1] Start: راه‌اندازی تانل
- [2] Stop: توقف تانل
- [3] Restart: ری‌استارت تانل
- [4] Edit: ویرایش تنظیمات
- [5] Delete: حذف تانل

## مشاهده لاگ‌ها 📊

گزینه 4 در منوی اصلی → انتخاب تانل

خودکار 50 خط آخر لاگ را نمایش می‌دهد.

## دستورات مفید 💡

```bash
# مشاهده وضعیت تمام تانل‌ها
systemctl status backhaul-*

# مشاهده لاگ یک تانل خاص
journalctl -u backhaul-iran-443 -f

# چک کردن پورت‌های باز
ss -tuln | grep LISTEN

# تست اتصال
curl -I http://localhost:2060
```

## رنگ‌های منو 🎨

- 🌸 **صورتی**: گزینه‌های اصلی
- 🔵 **فیروزه‌ای**: گزینه‌های ثانویه
- 💛 **زرد**: ورودی‌ها
- 🧡 **نارنجی**: اطلاعات
- 💙 **آبی**: جزئیات
- 💚 **سبز زیتونی**: بازگشت
- 🟢 **سبز**: موفقیت
- 🔴 **قرمز**: خطا و خروج

## نیازمندی‌ها 📋

- Ubuntu 20.04+ یا Debian 10+
- دسترسی Root
- حداقل 512MB RAM
- اتصال اینترنت فعال

## به‌روزرسانی 🔄

```bash
# حذف نسخه قبلی
rm -f /usr/local/bin/goldip /usr/local/bin/gip

# نصب مجدد
bash <(curl -Ls https://raw.githubusercontent.com/skyboy610/goldip/main/install.sh)
```

## حذف کامل 🗑️

```bash
# از داخل منو
goldip → گزینه 6 (Uninstall)

# یا دستی
rm -f /usr/local/bin/goldip /usr/local/bin/gip
rm -rf /root/backhaul-*
systemctl stop backhaul-*
systemctl disable backhaul-*
rm -f /etc/systemd/system/backhaul-*.service
```

## عیب‌یابی 🔍

### مشکل: دانلود فایل
```bash
# تست اتصال به گیتهاب
ping raw.githubusercontent.com

# استفاده از پروکسی
export https_proxy=http://your-proxy:port
```

### مشکل: Permission Denied
```bash
# اجرا با sudo
sudo bash <(curl -Ls https://raw.githubusercontent.com/skyboy610/goldip/main/install.sh)
```

### مشکل: تانل Start نمی‌شود
```bash
# چک لاگ
journalctl -u backhaul-* -n 50

# چک پورت
ss -tuln | grep PORT_NUMBER
```

## پشتیبانی 💬

- GitHub: https://github.com/skyboy610/goldip
- Issues: https://github.com/skyboy610/goldip/issues

## لایسنس 📄

MIT License - استفاده آزاد برای همه

---

نسخه: 2.0  
سازنده: GOLDIP Team  
تاریخ: 2026

⭐ اگر از این پروژه استفاده کردید، حتماً یک Star بدید!
